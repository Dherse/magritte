[`p_create_info`] is a pointer to a
[`DebugUtilsMessengerCreateInfoEXT`] structure containing the
callback pointer, as well as defining conditions under which this
messenger will trigger the callback.