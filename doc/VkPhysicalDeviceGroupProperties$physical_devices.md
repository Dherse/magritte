[`physical_devices`] is an array of [`MAX_DEVICE_GROUP_SIZE`][`PhysicalDevice`] handles representing all physical devices in the
group.
The first [`physical_device_count`] elements of the array will be valid.