[package]
name = "magritte"
version = "0.1.0"
edition = "2021"
license-file = "../LICENSE"
repository = "https://github.com/Dherse/magritte"
readme = "../README.md"
keywords = [ "vulkan", "GFX", "GPU", "graphics", "bindings" ]
categories = [ "api-bindings", "game-development", "graphics", "rendering" ]
description = "A high performance, high level graphics API around Vulkan"

[dependencies]
